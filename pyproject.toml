[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
    "aiohttp==3.8.6",
    "PyYAML>=6.0.1",
    "pytest==7.2.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.20.3",
    "pytest-mock>=3.10.0",
]
